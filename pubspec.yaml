name: bloom_filter_builder
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  build: ^1.2.2
  dart_style: ^1.3.3
  args: ^1.5.2
  bloom_filter:
    git:
      url: https://github.com/amake/bloom_filter.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
